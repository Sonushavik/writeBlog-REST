<!DOCTYPE html>
<html lang="en">
<head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Document</title>
        <link rel="stylesheet" href="/css/index.css">
</head>
<body>
        <h1>Edit your post</h1>
        <div class="post">
                <div class="about">
                        <p class="username"><%= post.username %></p>
                        <p class="timestamp"><%=  post.published_at.toLocaleString() %></p>
                </div>
                <div class="contentBox">
                        <form method="post" action="/posts/<%= post.id %>?_method=PATCH">
                                title:<input type="text" name="newtitle" value="<%= post.title %>"><br><br>
                                content: <textarea name="newcontent" id="" rows="5" cols="40"><%= post.content %></textarea><br><br>
                                password:<input type="password" name="editpass" placeholder="enter correct password">
                                <button type="submit">submit</button>
                        </form>
                </div>
        </div>
</body>
</html>